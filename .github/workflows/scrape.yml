diff --git a/.github/workflows/scrape.yml b/.github/workflows/scrape.yml
index 80afca997eccdb2777dbfd36d5e552e13856e7cb..354965b0aeaed2c88516467ee30ebea7bd498745 100644
--- a/.github/workflows/scrape.yml
+++ b/.github/workflows/scrape.yml
@@ -1,45 +1,59 @@
-name: Daily Real Estate News Scrape
+name: Daily Real Estate Signal Scraper
 
-on:
+"on":
   schedule:
-    - cron: '30 23 * * *'
-  workflow_dispatch:
+    - cron: "30 23 * * *"
+  workflow_dispatch: {}
+
+permissions:
+  contents: write
 
 jobs:
   scrape-and-commit:
     runs-on: ubuntu-latest
-    permissions:
-      contents: write
 
     steps:
-      - name: Checkout repository
+      - name: Checkout
         uses: actions/checkout@v4
 
-      - name: Setup Python
+      - name: Set up Python
         uses: actions/setup-python@v5
         with:
-          python-version: '3.11'
+          python-version: "3.11"
 
       - name: Install dependencies
         run: |
+          echo "Workspace: ${GITHUB_WORKSPACE}"
+          ls -la "${GITHUB_WORKSPACE}"
+          if [ ! -f "${GITHUB_WORKSPACE}/requirements.txt" ]; then
+            echo "requirements.txt not found at repo root"
+            find "${GITHUB_WORKSPACE}" -maxdepth 3 -name "requirements.txt" -print
+            exit 1
+          fi
           python -m pip install --upgrade pip
-          pip install -r requirements.txt
+          pip install -r "${GITHUB_WORKSPACE}/requirements.txt"
 
       - name: Run scraper
         env:
           NAVER_CLIENT_ID: ${{ secrets.NAVER_CLIENT_ID }}
           NAVER_CLIENT_SECRET: ${{ secrets.NAVER_CLIENT_SECRET }}
           GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
-        run: python main.py
+        run: |
+          if [ ! -f "${GITHUB_WORKSPACE}/main.py" ]; then
+            echo "main.py not found at repo root"
+            find "${GITHUB_WORKSPACE}" -maxdepth 3 -name "main.py" -print
+            exit 1
+          fi
+          python "${GITHUB_WORKSPACE}/main.py"
 
-      - name: Commit and push updated CSV
+      - name: Commit and push news_data.csv
         run: |
           git config user.name "github-actions[bot]"
           git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
-          if [ -f news_data.csv ] && [ -n "$(git status --porcelain news_data.csv)" ]; then
-            git add news_data.csv
-            git commit -m "chore: update news data"
-            git push
-          else
-            echo "No changes to news_data.csv"
+          git add news_data.csv
+          if git diff --cached --quiet; then
+            echo "No changes to commit"
+            exit 0
           fi
+          git commit -m "chore: update news_data.csv"
+          git push
